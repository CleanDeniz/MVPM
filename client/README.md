# Client (Vercel-ready)
- Create a Vercel project, import this `client` folder.
- Add env: `VITE_API_BASE` -> your Render server URL.
- Set BotFather menu button WebApp URL -> Vercel domain of this client.
